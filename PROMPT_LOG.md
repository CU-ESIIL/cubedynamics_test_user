# Prompt log

This is the chronological record of agent requests and actual work in `cubedynamics_test_user`. Follow the logging policy in [AGENTS.md](AGENTS.md). This log begins with the repository-purpose update; it does not reconstruct earlier template history.

## 2026-08-28 — TASK-001 — Establish the naive-user testing workspace

- **Agent / role:** Codex, repository setup and documentation.
- **Phase:** Setup for naive-user acceptance testing; no package testing or maintainer triage.
- **Request:** Ensure an `AGENTS.md` file and a prompt log track what agents are asked to do and what they actually do; define this repository as a naive tester of CubeDynamics using public functions and documentation without backend access; replace template documentation and website content with the current purpose. [Original request](prompts/2026-08-28-repository-setup.txt).
- **Supplied material:** [Full acceptance-run protocol](prompts/2026-08-28-acceptance-protocol.txt). Preserved as a future-run brief. This entry records the user's immediate repository/documentation request, not execution of the larger acceptance experiment.
- **Constraints:** Inspect this repository only; treat CubeDynamics as an external installed package; preserve the existing minimal MkDocs/GitHub Pages structure and historical policies.
- **Plan:** Extend existing agent rules, establish this log, describe the two testing phases and evidence requirements, update website identity/navigation/content, and validate the site.
- **Inspection:** Read this repository's `AGENTS.md` first, then the attached protocol, README, MkDocs configuration, website pages/styles/logo, and workflows. Found a clean working tree, an existing generic `AGENTS.md`, template branding, no prompt log, and no acceptance harness or reports.
- **Decisions:** Retain MkDocs and GitHub Pages; retain `docs/examples.md` as the testing-workflow route; distinguish setup from actual testing; require a separate explicit authorization before any later source-based triage.
- **Changes / outputs:** Extended [AGENTS.md](AGENTS.md) with the requested boundary, allowed/forbidden introspection, evidence freezes, and task logging while retaining the original general policies. Updated [README.md](README.md), [home](docs/index.md), and the existing [workflow route](docs/examples.md). Added [evidence guidance](docs/evidence.md) and [project notes / decision history](docs/project.md). Updated `mkdocs.yml` identity, URLs, and navigation; fixed logo navigation and homepage card indentation; excluded raw theme templates from published output. Added ignore rules for local environments and generated website output. Preserved both supplied prompts in `prompts/`.
- **Environment / attempts:** Host Python 3.14.3 had no MkDocs installed. Created `/tmp/cubedynamics-test-user-docs-venv` and installed the existing `requirements.txt` there: MkDocs 1.6.1 and Material 9.7.7. The first pip attempt failed because sandbox network access was unavailable; the approved retry succeeded. Local HTTP serving also required an approved retry after a sandbox bind denial. No CubeDynamics dependency was installed.
- **Other failures and recovery:** An initial combined patch was rejected because it targeted README twice and made no repository edits. The first link-check script mishandled GitHub Pages' absolute base path on the 404 page; corrected the checker. It also found raw override templates copied into the built site; excluded those build inputs and rebuilt. Browser inspection found inherited card indentation splitting the cards; corrected it. A browser heading query was ambiguous because the wrapper did not support its level filter; used exact heading names and continued.
- **Validation:** `/tmp/cubedynamics-test-user-docs-venv/bin/python -m mkdocs build --strict --clean --site-dir dist` passed after corrections. The theme printed its advisory about future MkDocs 2.0 changes, but the build completed successfully. An ad hoc HTML/Markdown link check passed for all 5 rendered HTML pages (4 content pages plus 404) and 121 local, anchor, and repository-file references before the final log update; verified that no raw theme templates were published and that the saved acceptance protocol matches the attachment byte-for-byte. In-app browser checks visited all four content routes, clicked navigation, confirmed the logo returns home, and visually checked the corrected homepage. `git diff --check` passed. No persistent test suite was added for this documentation-only change.
- **Limitations / not run:** No upstream CubeDynamics source, tests, developer files, or installed implementation were inspected. No acceptance test, package installation, live data retrieval, container run, upstream issue/PR, commit, push, or deployment was performed. Remote link availability and GitHub Pages settings were not checked; this task verifies local website output only. Inherited manual template-sync and JupyterLab-image workflows were retained and their limitations documented.
- **CubeDynamics artifact / environment / verdict:** None selected or installed. No Linux acceptance environment, exploratory operations, systematic coverage, or package verdict in this task.
- **Status:** Repository-purpose, policy, logging, and website updates completed and locally verified. The saved full acceptance experiment remains unexecuted.

## 2026-08-28 — TASK-002 — Black-box acceptance run

- **Agent / role:** Codex, black-box test coordinator; a fresh exploratory tester context is planned as requested in the saved acceptance protocol.
- **Request:** Execute naive exploratory and systematic public-surface testing; freeze evidence and report; draft issues only from observable behavior; stop without maintainer triage. [Run-specific instruction](prompts/2026-08-28-black-box-only.txt) supersedes the triage portion of the [original protocol](prompts/2026-08-28-acceptance-protocol.txt).
- **Boundary:** No CubeDynamics source, tests, developer files, agent instructions, prompt log, implementation modules, fixtures, editable installs, source checkouts, package patches, internal diagnosis, issues, or pull requests at any point in this run.
- **Intended work:** Establish an external installed artifact in clean Linux; record exploration chronologically and freeze it before systematic inventory/execution; freeze raw evidence and report, then draft user-perspective issues and update project status.
- **Initial state:** Read this repository's `AGENTS.md`, `PROMPT_LOG.md`, README, workflow and evidence policy. Working tree was clean. No package artifact, runner, container definition, or previous acceptance evidence is supplied in this repository.
- **Delegated task:** `naive_explorer` is a fresh-context Phase A scientist, as requested by the original protocol. It receives the public entry URL, policy/logging instructions and scientific goal, with no API inventory or development history. It owns chronological Phase A evidence and its narrative; the coordinator owns environment setup, later systematic coverage and this log.
- **Environment preparation:** Docker client 29.2.1 was available, but sandbox access required approval and the daemon was initially stopped. Approved `docker desktop start` succeeded. The official `python:3.11-slim` image was not cached; download is in progress. No upstream checkout is used.
- **Status:** In progress. Package version, environment, actual coverage and verdict are not yet established.

## 2026-08-31 — TASK-003 — First public-release acceptance test (`0.1.0rc1`)

- **Agent / role:** Codex, black-box coordinator; a new fresh-context naive scientist will own Phase A.
- **Request:** Independently install and test `cubedynamics==0.1.0rc1` as a public PyPI release, then systematically exercise its public grammar, real scientific questions, plausible randomized use, and novice errors. Freeze Phase A and the complete report before stopping; update this repository and commit it. [Full request](prompts/2026-08-31-rc1-acceptance-protocol.txt).
- **Boundary:** Public PyPI artifact, public website/README, allowed public introspection and observed behavior only. No upstream checkout, source/tests/fixtures/developer files, installed implementation, disassembly, source-following, editable installs, patches, triage, upstream issues or pull requests.
- **Prior-run handling:** The 2026-08-28 Phase A is cryptographically frozen and remains historical. It tested the pre-release public state and cannot represent `0.1.0rc1`. TASK-003 will use a new run directory, clean container, and new tester context with no prior findings, API matrix, or known failure modes.
- **Planned work:** Prepare a source-free Linux environment; run and freeze naive discovery; inventory only then; execute public grammar/scientific/error/randomized coverage with fixed seed; report, freeze, update project status, validate, and commit this repository only.
- **Actual execution:** Created isolated Debian 13.6/aarch64 Python 3.11.16 container `cubedynamics-rc1-naive-20260831` with no mounts or `PYTHONPATH`. Fresh agent `rc1_naive_scientist` ran 17 chronological operations. PyPI installation of `0.1.0rc1` succeeded; clean import failed for missing `libexpat.so.1`; normal apt recovery failed under dropped capabilities; local Debian-library unpacking plus `LD_LIBRARY_PATH` enabled import. A default/monthly PRISM example failed, a five-day daily real PRISM retrieval succeeded, NetCDF export failed on boolean attributes, and a spatial-mean pipe with explanation/validation/trace/unwrap/plot succeeded.
- **Evidence:** `artifacts/2026-08-31-rc1/phase_a/` and `reports/2026-08-31-rc1/naive_session.md` were frozen after agent handoff. Final limited-coverage report: `reports/2026-08-31-rc1/naive_user_report.md`; metrics: `artifacts/2026-08-31-rc1/metrics.json`.
- **Limitations:** Exhaustive systematic noun/source/verb execution, ten original questions, randomized exploration, and deliberate novice-error matrix were not completed and are reported as zero/not complete. No maintainer triage or upstream inspection/modification occurred.
- **Status / verdict:** Completed and frozen as a bounded first public-release black-box run. **FIX BEFORE HUMAN OUTSIDE TEST** (1 BLOCKER, 2 MAJOR, 1 DOCUMENTATION finding).

## 2026-09-03 — TASK-004 — Strict black-box rc3 outside-user acceptance test

- **Agent / role:** Codex, outside-user black-box acceptance tester.
- **Phase:** New `0.1.0rc3` acceptance run; naive exploration followed by systematic testing; no maintainer triage.
- **Request:** Independently install and assess the exact public PyPI release `cubedynamics==0.1.0rc3`; preserve a chronological and frozen Phase A before Phase B; attempt at least ten realistic scientific questions; explicitly retest first use, export, operation order, units, metadata, plotting, temporal support, events, and provenance; freeze a complete report; compare with rc1 only afterward; commit the assessment and stop. [Run protocol](prompts/2026-09-03-rc3-acceptance-protocol.txt).
- **Boundary / constraints:** This repository, the public PyPI artifact, public-facing CubeDynamics documentation/README, public distribution metadata/import path, and ordinary user introspection only. No upstream source checkout or files, installed implementation reading, tests, fixtures, developer material, source/disassembly inspection, editable install, monkeypatch, package modification, upstream issue, pull request, or implementation proposal. Preserve earlier evidence and avoid large data commits.
- **Intended work:** Create a fresh source-free environment; install and verify rc3; record environment and exact first-use behavior; capture and freeze chronological naive exploration before building a public API/scientific coverage inventory; run systematic questions and regressions; freeze artifacts/report; compare only then with frozen rc1 evidence; update repository documentation/history as appropriate; validate and commit.
- **Initial state:** `AGENTS.md`, the complete attached request, and the existing prompt log were read before substantive work. The working tree was clean and retained separate rc1 evidence plus a small acceptance capture/freeze harness. The existing log necessarily disclosed only the high-level rc1 verdict and four summary findings; no rc1 report, rc1 evidence details, upstream implementation, or private/developer material was read before Phase A. Because the coordinating tester has this minimal prior-run exposure, Phase A will rely only on rc3 public materials and chronologically preserve every discovery; the exposure is recorded rather than hidden.
- **Environment and installation:** Created source-free container `cubedynamics-rc3-naive-20260903` from official `python:3.11-slim` image ID `sha256:1042b61448fef4ba92d16a8c7eb4996d027568ce64792a7877fd88511e0af7c6`, Debian 13.6/aarch64, Python 3.11.16, pip 24.0, no mounts or `PYTHONPATH`, all capabilities dropped, 2 GiB memory. Exact `python -m pip install cubedynamics==0.1.0rc3` succeeded from PyPI in 36.3 s; `pip check` passed; distribution and import-facing versions both reported `0.1.0rc3` from normal `site-packages`.
- **First import / runtime:** `import cubedynamics` returned success without recovery but warned that the rasterio xarray backend could not load because `libexpat.so.1` was missing. Every fresh process repeated the warning and direct `import rasterio` failed. No system-library or package recovery was applied because the main rc3 workflows remained testable.
- **Phase A actual:** Used only public web material and allowed public names/signatures/docstrings. The exact search-discovered Getting Started PRISM request (daily precipitation, 2000–2020) returned real `(7671, 3, 3)` data in ~30 s. Noun/source discovery, a noun-first real PRISM mean, live state/trace/explain/validate, a 3-D viewer, NetCDF export/reopen, and both threshold/mean orders succeeded. Recorded mixed-generation docs, the persistent runtime warning, and stale temporal/dimension attrs after reduction. Two Docker-copy failures from tmpfs were preserved as harness limitations; moving the already-generated viewer to the container root allowed evidence preservation without changing CubeDynamics. Phase A plus `reports/2026-09-03-rc3/naive_session.md` was frozen and verified in `artifacts/2026-09-03-rc3/phase_a/FROZEN.json` before Phase B.
- **Phase B actual:** Public grammar inventory initially failed when the exposed `verbs.combine` proved to be a module rather than callable; a follow-up completed the intended signature discovery. Real PRISM temperature/precipitation and gridMET VPD were used for 11 questions covering means, cell-wise unusual warmth, hot/dry overlap, operation order, multi-variable identity, local events, regional consolidation, event metrics/intensity, timing synchrony, lagged association, and physical temporal support. The first systematic pass recorded 9 successes plus strict hot/dry rejection and its dependent lag failure. After preserving both failures, explicit label-policy and independent lag follow-ups completed the scientific coverage with caveats. Final question classification: 9 answered, 2 partial, 0 unresolved failures.
- **Regression results:** The prominent PRISM first-use path passed. Mean and Boolean NetCDF export/reopen passed; Boolean state was safely encoded as flagged int8. DataArray, Dataset condition, Boolean, selected multi-variable Dataset, and aggregate plots rendered. Threshold→mean and mean→threshold were both expressible and accurately explained. Mean `degC`, variance `degC^2`, and prevalence `proportion` were sensible. Major findings were: unusable rasterio after clean install; Boolean state/overlap unit conflicts; stale temporal/dimension attrs after reductions and export; and false strict-support rejection for PRISM temperature versus precipitation despite identical interval-defining fields. Moderate findings covered multi-input/relationship provenance loss, omitted exported trace, and mixed-generation public documentation; noisy/module-like public discovery was minor.
- **Decisions and interpretation:** Classified Q3 and Q5 as partial: Q3 required an explicit label policy because strict support falsely rejected equivalent PRISM intervals; Q5 retained xarray variable names/units but lost Dataset-level semantic provenance. Q10 counted as answered because lag direction, unequal-support caveat, and non-causal semantics were explicit. Did not treat newly observed rc3 issues as regressions where rc1 lacked coverage. Selected **FIX BEFORE HUMAN OUTSIDE TEST** with 0 blocker, 4 major, 3 moderate, and 1 minor finding.
- **Freezes and comparison:** Froze and verified environment (9 files), Phase B plus the detailed rc3-only assessment (22 files), and the earlier Phase A (16 files) before opening the rc1 report. Only then compared against frozen rc1 evidence. rc3 improved outright CubeDynamics import, the obvious PRISM path, Boolean NetCDF export, and systematic scientific coverage, while the rasterio runtime remained unusable. Final full-run manifest `artifacts/2026-09-03-rc3/FROZEN.json` verifies 52 files, including all three reports and metrics.
- **Changes / outputs:** Added three public-surface reproduction scripts under `acceptance/`; preserved ~1.1 MB of command records, structured JSON, two NetCDFs, and small HTML viewers under `artifacts/2026-09-03-rc3/`; added chronological, pre-comparison detailed, and final comparison reports under `reports/2026-09-03-rc3/`; updated README and website current-state/workflow/evidence pages without replacing earlier history.
- **Validation:** All four freeze manifests verified; all three new acceptance scripts parsed with Python AST; `git diff --check` passed; a strict clean MkDocs 1.6.1 / Material 9.7.7 build to `/tmp/cubedynamics-rc3-site` passed. The first temporary dependency installation attempt failed under sandbox DNS restrictions; the approved retry succeeded. Material printed its upstream MkDocs 2.0 advisory, but the strict build completed. No generated website output was committed.
- **Limitations / not run:** This was focused acceptance coverage, not every public callable or every noun/source combination. Sentinel-2 and raster-backed workflows were not pursued after the independently confirmed runtime import failure. No randomized test matrix, credentials, huge downloads, upstream source/tests/developer material, installed implementation inspection, package patch, maintainer triage, issue, pull request, or deployment was performed. Public documentation was accessed through ordinary web search/pages; public data rights/citation remain those of PRISM Climate Group and gridMET as recorded in package metadata and report links.
- **Artifact / verdict:** Public PyPI `cubedynamics==0.1.0rc3`. Primary report: `reports/2026-09-03-rc3/naive_user_report.md`; detailed rc3-only report: `reports/2026-09-03-rc3/rc3_assessment_before_comparison.md`; primary evidence: `artifacts/2026-09-03-rc3/`. **FIX BEFORE HUMAN OUTSIDE TEST.** The complete frozen assessment and synchronized documentation were committed as `14824f5` (`Add frozen CubeDynamics rc3 acceptance assessment`).
- **Status:** Completed and committed. Testing stopped at the required black-box boundary before maintainer triage, upstream issue filing, or package changes.
